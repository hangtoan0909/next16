export type InspectionAgencyType = {
  dailyCapacity: number;
  serviceArea?: number | null;
};

export type InspectionAgencyPayloadType = {
  dailyCapacity: number;
  serviceArea?: number | null;
};
